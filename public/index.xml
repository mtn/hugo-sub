<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>mtn</title>
    <link>https://mtn.github.io/index.xml</link>
    <description>Recent content on mtn</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-US</language>
    <copyright>Copyright © 2017 Michael Noronha</copyright>
    <lastBuildDate>Fri, 31 Mar 2017 17:16:25 -0500</lastBuildDate>
    <atom:link href="https://mtn.github.io/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>Projects</title>
      <link>https://mtn.github.io/projects/projects/</link>
      <pubDate>Fri, 31 Mar 2017 17:16:25 -0500</pubDate>
      
      <guid>https://mtn.github.io/projects/projects/</guid>
      <description>&lt;p&gt;Some of the personal projects I&amp;rsquo;ve worked on accompanied by descriptions (aka abbreviated versions of their README).&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Snake with GNU ncurses! -- Part I, ncurses basics</title>
      <link>https://mtn.github.io/posts/snake_part1/</link>
      <pubDate>Tue, 28 Mar 2017 21:39:31 -0500</pubDate>
      
      <guid>https://mtn.github.io/posts/snake_part1/</guid>
      <description>

&lt;p&gt;As my first game, I made snake in C using the graphics library ncurses! There are probably several dozen other languages that would have also sufficed or been better but I chose C because I picked it up pretty recently, so making a project from scratch was a decent challenge. Over two posts, I&amp;rsquo;ll walk through some of the process, including decisions I made along the way and the rationale behind them. In this one, I&amp;rsquo;ll set up the basic features of ncurses that I found useful. What I write should be &lt;em&gt;mostly&lt;/em&gt; self-contained, but the links should clear things up in cases where I&amp;rsquo;m not.&lt;/p&gt;

&lt;h1 id=&#34;what-s-ncurses&#34;&gt;What&amp;rsquo;s ncurses?&lt;/h1&gt;

&lt;p&gt;ncurses is probably the easiest graphics library to get started with (not that I have much other experience yet), and is very well documented &lt;a href=&#34;http://tldp.org/HOWTO/NCURSES-Programming-HOWTO/&#34;&gt;here&lt;/a&gt;. It came about as a successor to curses, a wrapper over terminal graphics functionality that allows us to render pretty things on different terminals without working with messy raw values. To the best of my knowledge, the necessary header is included with the Xcode developer tools for MacOS, and is available through &lt;code&gt;apt-get&lt;/code&gt; (or your default package manager) on linux. To make something simple like snake, there are only a few key concepts you need to wrap your mind around. Starting simple is good; I first stumbled on ncurses after googling &amp;ldquo;C terminal graphics&amp;rdquo; when working on a (not-yet-finished) Conway&amp;rsquo;s Game of Life simulator, and felt I was making too many decisions because I wanted to progress in the project without getting a feel for the full library. There&amp;rsquo;s lots more, though, which I&amp;rsquo;ll definitely be exploring in the future and you should check out after reading this post too!&lt;/p&gt;

&lt;h2 id=&#34;initialization-functions&#34;&gt;Initialization Functions&lt;/h2&gt;

&lt;p&gt;To initialize our ncurses session, we first call &lt;code&gt;initscr()&lt;/code&gt;. There are also a few functions we call at the start our program in order to define basic behaviors:&lt;/p&gt;

&lt;p&gt;&lt;code&gt;cbreak()&lt;/code&gt; &amp;ndash; Disables line buffering and also allows us to process control sequences (eg. CTRL^C)&lt;/p&gt;

&lt;p&gt;&lt;code&gt;noecho()&lt;/code&gt; &amp;ndash; Turns off input echoing, as its name suggests :). This means that the keys you press will not be printed on the screen like they normally are when you type into the terminal&lt;/p&gt;

&lt;p&gt;&lt;code&gt;keypad(stdscr,TRUE)&lt;/code&gt; &amp;ndash; Allows us to enable processing of additional keys like the function keys and more importantly, the arrow keys (unless you like to play snake with WASD or something, I guess). Note the first argument, &lt;code&gt;stdscr&lt;/code&gt; can be varied if we want to do the same thing for windows, which I&amp;rsquo;ll talk about soon&lt;/p&gt;

&lt;p&gt;&lt;code&gt;curs_set(0)&lt;/code&gt; &amp;ndash; Hides the cursor, which we wouldn&amp;rsquo;t want distracting from our beautifully designed game ;)&lt;/p&gt;

&lt;p&gt;&lt;code&gt;halfdelay()&lt;/code&gt; &amp;ndash;  Sets an upper bound on the amount of time the program waits for input (after which it returns ERR). Sounds useful, right?&lt;/p&gt;

&lt;h2 id=&#34;output-and-input&#34;&gt;Output and Input&lt;/h2&gt;

&lt;p&gt;ncurses essentially gives you a grid systems of rows and columns. Within this basic grid, we can do simple things like move around and print characters but, as we&amp;rsquo;ll see, also introduce more additional structures. As with many similar programs, the top left corner of the screen is (0,0) rather than the bottom left as is conventional elsewhere. What is rather more unique is that throughout ncurses where coordinate arguments are used, the &amp;ldquo;y&amp;rdquo; is specified before the &amp;ldquo;x.&amp;rdquo; A few functions!&lt;/p&gt;

&lt;h3 id=&#34;our-output-functions&#34;&gt;Our Output Functions&lt;/h3&gt;

&lt;p&gt;&lt;code&gt;move(y,x)&lt;/code&gt; &amp;ndash; Allows us to move the cursor to a specified location on the screen. Note that if we hide the cursor (call &lt;code&gt;curs_set(0)&lt;/code&gt;), this doesn&amp;rsquo;t produce any externally-observable change&lt;/p&gt;

&lt;p&gt;&lt;code&gt;addch(ch | ATTRIBUTES)&lt;/code&gt; &amp;ndash; Adds a character at the current location of the cursor. Note that we can OR &lt;a href=&#34;http://www.tldp.org/HOWTO/NCURSES-Programming-HOWTO/attrib.html&#34;&gt;attributes&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mvaddch(y, x, ch)&lt;/code&gt; &amp;ndash; A composition of the prior two functions&lt;/p&gt;

&lt;p&gt;&lt;code&gt;printw(str)&lt;/code&gt; &amp;ndash; The &lt;code&gt;printf&lt;/code&gt; of ncurses&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mvprintw(y, x, str)&lt;/code&gt; &amp;ndash; Similar to &lt;code&gt;mvaddch&lt;/code&gt;, a composition of &lt;code&gt;move&lt;/code&gt; and &lt;code&gt;printw&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;attron(ATTR_X)&lt;/code&gt; &amp;ndash; Allows us to &amp;ldquo;switch on&amp;rdquo; an attribute that impacts how text is displayed (eg. A_BOLD). Turned off by &lt;code&gt;attroff&lt;/code&gt;.&lt;/p&gt;

&lt;h3 id=&#34;our-input-functions&#34;&gt;Our Input Functions&lt;/h3&gt;

&lt;p&gt;&lt;code&gt;getch()&lt;/code&gt; &amp;ndash; Reads a character from the terminal. Note that unless &lt;code&gt;cbreak&lt;/code&gt; or &lt;code&gt;raw&lt;/code&gt; are called, input will be newline-buffered&lt;/p&gt;

&lt;p&gt;And actually, that&amp;rsquo;s the only one! There are certainly others with fairly self-explanatory names, such as &lt;code&gt;scanw&lt;/code&gt;, but snake is relatively simple and we won&amp;rsquo;t be needing them :).&lt;/p&gt;

&lt;h2 id=&#34;a-few-others&#34;&gt;A Few Others&amp;hellip;&lt;/h2&gt;

&lt;p&gt;There are a few other functions that we&amp;rsquo;ll find useful. One of these is &lt;code&gt;getmaxyx(stdscr,y,x)&lt;/code&gt;, which allows us to get the dimensions of our current screen (or window, if we alter the first parameter). We can use these numbers to calculate the coordinates we&amp;rsquo;ll render elements of our interface.&lt;/p&gt;

&lt;h2 id=&#34;windows&#34;&gt;Windows&lt;/h2&gt;

&lt;p&gt;When we call &lt;code&gt;initscr&lt;/code&gt;, we are given a clean canvas to work with &amp;ndash; the entire terminal. Windows allow us to add additional &amp;ldquo;layers&amp;rdquo; to this canvas, which is useful if we want to manage separate parts of the display independently. Example cases would be the game itself or a score display, which we might want to update multiple times per second asynchronously. There are a few special functions, though on the whole windows behave mostly in a similar way to the screen itself:&lt;/p&gt;

&lt;p&gt;&lt;code&gt;newwin(height, width, starty, startx)&lt;/code&gt; &amp;ndash; Initializes a new window at the coordinates &lt;code&gt;(starty,startx)&lt;/code&gt; relative to &lt;code&gt;stdscr&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;delwin(win)&lt;/code&gt; &amp;ndash; Deletes a window, deallocating its memory&lt;/p&gt;

&lt;p&gt;Apart from these two functions, windows can mostly be manipulated by functions named after their standard counterparts with a &amp;ldquo;w&amp;rdquo; appended to the start. For example, &lt;code&gt;printw&lt;/code&gt; becomes &lt;code&gt;wprintw&lt;/code&gt;. The first argument to all these functions is the some window.&lt;/p&gt;

&lt;h2 id=&#34;refresh&#34;&gt;Refresh&lt;/h2&gt;

&lt;p&gt;Congrats! You&amp;rsquo;re well on your way to snake in ncurses. But if you&amp;rsquo;ve tried anything out yet, you&amp;rsquo;ll have realized there&amp;rsquo;s a problem. Maybe you have the wrong version&amp;hellip;? No! I&amp;rsquo;ve just left the most important part for last :). On this last point, it&amp;rsquo;s helpful to think of a virtual screen that exists separately from your physical screen. Changes we make go into effect immediately on the virtual screen &amp;ndash; if we print something to the screen, for example &amp;ndash; but we need to push them onto the physical screen somehow. This is where &lt;code&gt;refresh()&lt;/code&gt; comes in (and correspondingly, &lt;code&gt;wrefresh&lt;/code&gt;)! Whenever we make a change, we need to &lt;em&gt;refresh&lt;/em&gt; our screen in order to see it in real life. When in doubt, check if you&amp;rsquo;ve done this.&lt;/p&gt;

&lt;h2 id=&#34;that-was-a-long-list&#34;&gt;That was a long list&amp;hellip;&lt;/h2&gt;

&lt;p&gt;&amp;hellip;so it&amp;rsquo;s time for an example (built on top of code from Papdala&amp;rsquo;s guide)! I&amp;rsquo;ll talk through building a menu, and we&amp;rsquo;ll be able to reuse a lot of this code in our eventual program. First, let&amp;rsquo;s use our initialization functions to set up the environment:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-c&#34;&gt;#include &amp;lt;ncurses.h&amp;gt;
#include &amp;lt;string.h&amp;gt;

int main()
{
    initscr();                    // Start curses mode
    noecho();                     // Input is not echoed to the screen
    curs_set(0);                  // Hides the cursor
    cbreak();                     // Input isn&#39;t line-buffered

    int yMax, xMax, menuHeight=10, menuWidth=40;
    getmaxyx(stdscr,yMax,xMax);             // Stores the windows dimensions

    // Creates a new window W, calculating it&#39;s position from
    WINDOW *W = newwin(menuHeight,menuWidth,(yMax-menuHeight)/2,(xMax-menuWidth)/2);
    keypad(W,TRUE);
    refresh();
    box(W,0,0);

    wattron(W,A_REVERSE);
    char* title = &amp;quot;Title&amp;quot;;
    mvwprintw(W,1,(menuWidth-strlen(title))/2,title);
    wattroff(W,A_REVERSE);

    // Variable highlight determines which menu item is emphasized
    int highlight = 0, numOptions = 3;
    char* options[3] = { &amp;quot;Option 1&amp;quot;, &amp;quot;Option 2&amp;quot;, &amp;quot;Option 3&amp;quot; };
    while(true){
        for(int i = 0; i &amp;lt; numOptions; ++i){
            if(i==highlight){
                wattron(W,A_BOLD);           // Bold currently highlighted option
                mvwprintw(W,5+i,5,&amp;quot;*&amp;quot;);      // A asterisk for more emphasis
            }
            else
                mvwprintw(W,5+i,5,&amp;quot; &amp;quot;);      // Clear old asterisks
            mvwprintw(W,5+i,6,options[i]);   // Print each option
            wattroff(W,A_BOLD);              // Turn off bold, if it was on
        }
        int input = wgetch(W); // Get input and store it in the variable input

        switch(input){
            case KEY_DOWN:
                if(highlight &amp;lt; 2) highlight++;  // Check, then move highlight down
                break;
            case KEY_UP:
                if(highlight &amp;gt; 0) highlight--;  // Check, then move highlight up
            default:
                break;
        }
        wrefresh(W);            // Refreshes the window

        // A return value of 10 from wgetch indicates that enter was pressed
        if(input==10) break;
    }

    // Move to the bottom left and print
    mvprintw(yMax-1,0&amp;quot;The option selected was %d&amp;quot;,highlight+1);

    getch();                      // Wait for input before proceeding
    delwin(W);                    // Deletes the window and deallocates its memory
    endwin();                     // End curses mode
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We first initialized our environment, entering &lt;code&gt;curses&lt;/code&gt; mode. The menu is rendered using a window, W, and continuously updated using a while loop. Input is gathered from the keyboard at each iteration,  with the loop terminating once the enter key is pressed (input == 10). The value of highlight at the time that enter is pressed indicates the user&amp;rsquo;s choice, which we print to the screen with &lt;code&gt;printw&lt;/code&gt;. Usually, we&amp;rsquo;d have to call &lt;code&gt;refresh&lt;/code&gt; to have it actually display, but it turns out that &lt;code&gt;getch&lt;/code&gt; already does this for us, so we&amp;rsquo;re done!&lt;/p&gt;

&lt;p&gt;Wow, that was a lot! Hopefully the example helped draw everything together. Don&amp;rsquo;t worry if you can&amp;rsquo;t remember more than a few of the functions presented above &amp;ndash; as you start to work with ncurses, you&amp;rsquo;ll quickly internalize them. Until then, I hope this will be a good reference. If you&amp;rsquo;re feeling adventurous, poke around for other functions in the &lt;a href=&#34;http://www.tldp.org/HOWTO/NCURSES-Programming-HOWTO/attrib.html&#34;&gt;how-to&lt;/a&gt; and build upon the example I provided. Good luck!&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Snake with ncurses</title>
      <link>https://mtn.github.io/projects/snake/</link>
      <pubDate>Mon, 27 Mar 2017 17:21:16 -0500</pubDate>
      
      <guid>https://mtn.github.io/projects/snake/</guid>
      <description>

&lt;h1 id=&#34;snake&#34;&gt;Snake&lt;/h1&gt;

&lt;p&gt;A terminal implementation of snake built in C with &lt;a href=&#34;https://www.gnu.org/software/ncurses/&#34;&gt;GNU ncurses&lt;/a&gt;. This was both my first graphical (sorta) application in C, and also my first game. It features multiple levels, with different rates of movement and score increases per food consumed. As far as I can tell, the rules I&amp;rsquo;ve implemented are consistent with typical snake games.&lt;/p&gt;

&lt;p&gt;There were a few minor things I didn&amp;rsquo;t love and would consider changing:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;I manipulated &lt;code&gt;halfdelay()&lt;/code&gt; (combined with a timer) to get the behavior I wanted, but hard is still pretty easy. &lt;code&gt;halfdelay()&lt;/code&gt; only takes integer inputs, so I&amp;rsquo;d have to come up with a different solution to address this.&lt;/li&gt;
&lt;li&gt;Rows are taller than columns, so vertical movement is way faster. This wouldn&amp;rsquo;t be a huge deal to fix with a different timing system.&lt;/li&gt;
&lt;li&gt;Diamonds might not be the best character for snake body?&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;a href=&#34;https://github.com/mtn/snake&#34;&gt;Repository&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>about</title>
      <link>https://mtn.github.io/about/</link>
      <pubDate>Wed, 22 Mar 2017 00:38:03 -0500</pubDate>
      
      <guid>https://mtn.github.io/about/</guid>
      <description></description>
    </item>
    
  </channel>
</rss>